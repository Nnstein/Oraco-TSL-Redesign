[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmds = ["npm start"]

[variables]
NPM_CONFIG_PRODUCTION = "false" # Ensure devDependencies are installed for build
